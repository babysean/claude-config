# 서브에이전트 위임 정책

메인 세션이 **총괄(orchestrator)** 이다. 요청을 받으면 직접 처리할지 전문
에이전트에 위임할지 스스로 판단하고, 위임했더라도 최종 판단과 사용자
응답은 메인 세션이 한다. 위임 여부를 사용자에게 되묻지 않는다.

## 1. 직접 처리 — 위임하지 않는다

아래는 위임 비용(컨텍스트 재구성 + 왕복 지연)이 이득보다 크다. 그냥 한다.

- 파일 1개 범위의 국소 수정 (오타·포맷·import 정리·설정값 변경·상수 교체)
- 경로나 심볼을 이미 아는 검색, 단일 사실 확인
- 빌드·테스트·린트 실행, git 상태 확인, 로그 읽기
- 대화형 질문 ("이거 왜 이래?", "이 함수 뭐 하는 거야?")
- 사용자가 원인과 고칠 위치를 이미 지목했고, 그 수정이 한 파일에 닫히는 경우

**애매하면 위임한다.** 라우팅 표의 도메인에 해당하면 파일 수와 무관하게
전문가를 부른다. 직접 처리는 위 목록에 명확히 들어맞을 때만.

## 2. 위임 — 아래에 해당하면 판단해서 호출한다

- **2개 이상 파일**에 걸친 구현/리팩터링
- 새 기능·새 엔드포인트·새 컴포넌트·새 스키마의 구현 (파일 수 무관)
- 원인을 아직 모르는 버그 조사 — 해당 도메인 전문가에게 조사부터 맡긴다
- 어디를 봐야 할지 모르는 넓은 탐색 → `Explore`
- 코드 작성을 끝낸 뒤의 품질 점검 → `code-reviewer` (기본값으로 돌린다)
- 서로 의존 없는 작업 2개 이상 → **한 메시지에 동시 호출해 병렬 실행**
- 전문 판단이 필요한 영역 (아래 표)

백엔드/프론트가 함께 걸리면 `backend-engineer` 와 `frontend-engineer` 를
한 메시지에서 동시에 호출한다. 한쪽이 다른 쪽의 결과를 필요로 할 때만 순차 실행.

### 라우팅 표

| 상황 | 에이전트 |
|---|---|
| 시스템 설계, 기술 스택 결정, 대규모 리팩터링 계획 | `software-architect` |
| 서버 로직, REST/GraphQL API, 비즈니스 로직 | `backend-engineer` |
| UI 컴포넌트, 스타일링, 클라이언트 상태 관리 | `frontend-engineer` |
| 스키마 설계, 쿼리 튜닝, 마이그레이션 | `database-administrator` |
| 작성된 코드의 품질·버그 감사 | `code-reviewer` |
| 테스트 작성, 커버리지 분석, 테스트 전략 | `test-engineer` |
| 취약점 점검, 인증/인가 로직 검토, OWASP | `security-expert` |
| CI/CD, Docker/K8s, 배포, 인프라 | `devops-engineer` |
| 느린 원인 분석, 프로파일링, 병목 제거 | `performance-engineer` |
| ETL 파이프라인, 데이터 모델링, 스트림 처리 | `data-engineer` |
| ML 모델, LLM 연동, RAG, 프롬프트 설계 | `ml-ai-engineer` |
| README, API 문서, ADR, 체인지로그 | `technical-writer` |
| 낯선 레포 구조·흐름 파악 | `codebase-explainer` |
| 넓은 범위 코드 검색 | `Explore` |
| 구현 전략 수립 | `Plan` |

`project-manager` 는 이 정책이 대체한다. 사용자가 이름을 직접 부르거나,
여러 전문 분야가 얽힌 다단계 대형 작업일 때만 쓴다.

## 3. 위임할 때 지킬 것

- **컨텍스트를 실어 보낸다.** 서브에이전트는 이 대화를 못 본다. 목표,
  구체적 하위 작업, 관련 파일 경로, 제약을 프롬프트에 명시한다.
- **결과는 사용자에게 안 보인다.** 받은 내용 중 중요한 것을 메인 세션이
  직접 요약해 전달한다.
- **결과를 그대로 믿지 않는다.** 서브에이전트 보고가 틀릴 수 있다. 핵심
  주장은 근거 파일을 확인한다.
- 이미 위임한 검색을 메인 세션에서 중복 실행하지 않는다.
- `subagent_type` 은 라우팅 표의 이름을 접두사 없이 그대로 쓴다.
  (`backend-engineer`, `code-reviewer` — `my-` 를 붙이지 않는다.)

## 4. 프로젝트 전용 에이전트 우선

`<repo>/.claude/agents/` 의 정의가 `~/.claude/agents/` 보다 우선한다.
해당 레포에서 작업할 때는 그쪽을 먼저 본다.

- `IdeaProjects/dwe-itservice-repo` → `design-qa`, `reviewer`
- `dwe-platform` → `reviewer`
- `Personal/ditter` → `reviewer`
